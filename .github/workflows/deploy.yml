name: Deploy to Netlify

on:
  push:
    branches:
      - main
      - pre-prod
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run type-check

      - name: Unit tests
        run: npm run test:unit -- --run

      - name: Install Playwright (Chromium)
        run: npx playwright install --with-deps chromium

      - name: Build
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: E2E smoke tests
        run: npx playwright test --project="Desktop Chrome" e2e/smoke.spec.ts
        env:
          CI: true

      - name: Deploy to Netlify (production)
        if: github.ref == 'refs/heads/main'
        run: npx netlify-cli deploy --dir=dist --prod --message "Deploy from main branch"
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      - name: Deploy to Netlify (branch preview)
        if: github.ref != 'refs/heads/main'
        run: |
          BRANCH_NAME="${GITHUB_REF#refs/heads/}"
          npx netlify-cli deploy --dir=dist --alias="$BRANCH_NAME" --message "Deploy from $BRANCH_NAME branch"
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
